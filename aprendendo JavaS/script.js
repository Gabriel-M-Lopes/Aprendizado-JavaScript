/*
Expressões e operadores

- Expressions
- Operators
  Binary
  Unary
  Ternary
*/
////////////////////////////////////////////////////////
/*
  Operadores unários
  typeof
  delete
*/

// const person = {
//   name: 'Gabriel',
//   age: 18,
// }
// delete person.age
// console.log(typeof person)

////////////////////////////////////////////////////////

/*
  Operadores Aritméticos
  - multiplicação *
  - divisão / 
  - soma +
  - subtração -

  // resto da divisão %
      // let remainder
      // remainder = 100 % 43
      // console.log(remainder)
  // incremento ++
      // let increment = 0
      // increment++
      // console.log(++increment)
  // decremento --
      // let decrement = 0
      // decrement--
      // console.log(--decrement)
  // exponencial **
      // console.log(2 ** 2)
*/

////////////////////////////////////////////////////////

// Grouping operator () a mesma coisa que na matematica

////////////////////////////////////////////////////////

/*
Operadores de comparação

  Ira comparar valores e retornar um boolean como resposta a comparação

  // ==  igual a
    // let one = 1
    // let two = 2
  // console.log(two == 1)
  // console.log(one == "1")
  
  //  !=  diferente de
  // console.log(one != two)
  // console.log(one != 1)
  // console.log(one != '1')

  === Estritamente igual a
  !== Estritamente diferente de

  > maior que
  >= maior ou igual a
  < menor que
  <= menor ou igual a
*/

////////////////////////////////////////////////////////

/*
  Operadores de atribuição (Assignment)
      // let x
      // x = 1 
  - Addition assignment
      // x += 2 = (x = x + 2)
  - Subtraction assignment
      // x -= 1 = (x = x - 1)
  - Multiplication assignment
      // x *= 2 = (x = x * 2)
  - Division assignment
      // x /= 2 = (x = x / 2)
  - Remainder, exponetiation
      // x %= 2
      // x **= 2
*/

////////////////////////////////////////////////////////

/*
  Operadores lógicos (logical operators)
  - 2 valores booleanos, quando verificados, resultar em verdadeiro ou falso
   // let pao = true
   // let queijo = false
   // let presunto = true

    // And &&
      // console.log(pao && queijo && presunto)
    // Or ||
      // console.log(pao || queijo || presunto)
    // Not(negando) !
      // console.log(!queijo)
*/

////////////////////////////////////////////////////////

/*
  Operador condicional
  - Dependendo da condição, recebera um valor diferente

  - Condição então valor 1 se não valor 2
    // condition ? value1 : value2
      // let queijo = true
      // let pao = true
      // const cafe = pao&&queijo ? "Legal" : "Ruim"
      // console.log(cafe)
*/

////////////////////////////////////////////////////////

/*
  Operador de String (String operator)

  // comparasion

  // concatenation
  Retorna a união de duas strings
*/